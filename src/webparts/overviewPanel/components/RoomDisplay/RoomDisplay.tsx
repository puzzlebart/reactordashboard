
import * as React from 'react';
import styles from './RoomDisplay.module.scss';

export interface IRoomDisplayProps {}

export interface IRoomDisplayState {}

export default class Component extends React.Component<IRoomDisplayProps, IRoomDisplayProps> {
  public render(): React.ReactElement<IRoomDisplayProps> {
    return (
      <div>

      </div>
    );
  }


  private getLatestTempData() {

  }
}
